import { FiltroRicettePipe } from './filtro-ricette.pipe';

describe('FiltroRicettePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRicettePipe();
    expect(pipe).toBeTruthy();
  });
});
